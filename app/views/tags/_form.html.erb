<%= form_for(@tag) do |f| %>
  <% if @tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this user from being saved: </h2>
        <ul>
          <% @tag.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        </ul>
    </div>
  <% end %>
  
  <%= f.text_field :name %>
  
  <%= f.submit "Create Tag" %>

<% end %>