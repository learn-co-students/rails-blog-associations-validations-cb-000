<p id="notice"><%= notice %></p>

<p>
  <h3>Post Title:</h3>
  <%= @post.post_name %>
  <%= @post.post_content %>
  <% @tags.each do |tag| %>
    <%= tag.name %>
  <% end %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
